# The deterministic solver and test suite use only the Python standard library.
# This file intentionally has no required third-party dependencies.
#
# Optional LLM-backed evidence extraction/explanations:
# openai>=1.0.0
